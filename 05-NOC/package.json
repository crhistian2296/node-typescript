{
  "name": "05-NOC",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch --import tsx/esm src/app.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "docker:test": "docker compose -f docker-compose.test.yml --env-file .env.test up -d",
    "test": "pnpm run docker:test && vitest",
    "test:watch": "pnpm run docker:test && vitest --watch",
    "test:coverage": "pnpm run docker:test && vitest --coverage",
    "contract:emit": "prisma contract emit",
    "format:prisma": "prisma contract format"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.2",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@prisma/cli-engine": "0.2.3",
    "@types/node": "^26.0.1",
    "@types/nodemailer": "^8.0.1",
    "@vitest/coverage-v8": "4.1.11",
    "prisma": "8.0.0-rc.10",
    "rimraf": "^6.1.3",
    "tsx": "^4.22.4",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@prisma/orm-postgres": "8.0.0-rc.8",
    "cron": "^4.4.0",
    "dotenv": "^17.4.2",
    "env-var": "^7.5.0",
    "mongoose": "^9.9.4",
    "nodemailer": "^9.0.5",
    "vitest": "^4.1.11"
  }
}
